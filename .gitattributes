# Generated by MothershipCode's CI/CD hardener (Phase 4 contract-drift refactor).
#
# Marks the cross-tier codegen output so it reads as machine-generated, taming
# review/diff noise on PRs that touch the regenerated tree:
#   * linguist-generated=true → GitHub collapses these files in the PR diff and
#     excludes them from repo language stats.
#   * -diff → git itself omits their textual diff (shows "Binary files differ"),
#     so a large regenerated tree doesn't bury the human-authored change.
#
# This is COSMETIC ONLY. It cannot auto-resolve a merge conflict on generated
# code — regenerate-after-merge plus the contract-conformance gate remain the
# answer for that.
Generated/**            linguist-generated=true
Generated/**            -diff
contracts/*.openapi.yaml linguist-generated=true
